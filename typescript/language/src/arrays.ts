const activeUsers: string[] = ['Colt'];

let users: string[] = [];

users.push('arun');

const names: Array<string> = [];

type Point3D = {
  x: number;
  y: number;
  z: number;
};

const points: Array<Point3D> = [];
points.push({ x: 10, y: 20, z: 30 });

const randomNumbers: number[][] = [[2]];
